{% extends 'base.html.twig' %}

{% block title %}Mes Adresses - la boutique Espace Verte en ligne {% endblock %}

{% block content %}
    <br/>
    <h1>Mes Adresses</h1>
    <p>C'est dans cet espace que vous allez pouvoir gérer toutes vos adresses.</p>
    <div class="row">
        <div class="col-md-4">
            <a href="{{ path('account_address_add') }}"> Ajouter une adresse </a>
        </div>
        <div class="col-md-4">
            <a href="{{ path('account') }}"> Retour </a> 
        </div>
    </div>
    <hr>
    {# {{ dump(app.user)}}; #}
    {% if app.user.addresses|length == 0 %}
        <p class="text-center">
            Vous n'avez pas encore ajouté d'adresses dans votre compte client. Pour en ajouter une , veuillez  <a href="{{ path('account_address_add') }}">cliquer ici </a>
        </p>
    {% else %}
    <div class="row">
        {% for address in app.user.addresses %}
            <div class="col-md-4 ">
                <div class="card" style="width: 18rem;">
                    <div class="card-body border border-success rounded">
                        <h5 class="card-title">Adresse de livraison</h5>
                        <p class="card-text">
                            {{ (address.address) }},<br>
                            {{ (address.postal) }} - {{ (address.city) }} - {{ (address.country) }}
                        </p>
                        <a href="{{ path('account_address_edit', {'id': address.id })}}">Modifier</a> | <a href="{{ path('account_address_delete', {'id': address.id })}}">Supprimer</a>
                    </div>
                </div>
            </div>  
        {% endfor %}
        
    </div>
    <hr>      
    {% endif %}
      
{% endblock %}